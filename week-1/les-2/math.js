const a = 10; // number
const b = 20;

const sum = a + b;

// Alazar heeft 3 appels
let apples = 3;
console.log(apples);
// Alazar eet een appel
apples = apples - 1;
console.log(apples);
// Alazar eet nog een appel (kortere manier om dit te schrijven)
apples -= 1;
console.log(apples);
// Alazar gaat naar de winkel en koopt 3 extra appels
apples += 3;
console.log(apples);
// Alazar eet nog een appel (nog kortere manier, enkel indien +1 of -1)
apples--;
console.log(apples);
// Alazar krijgt een appel
apples++; // 1 toevoegen
console.log(apples);
